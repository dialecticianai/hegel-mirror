# theme/

Typography, spacing, colors, and layout configuration system.

## Structure

```
theme/
├── mod.rs              Theme struct (Typography, Spacing, Colors, Layout)
└── default.rs          Default theme constant
```
